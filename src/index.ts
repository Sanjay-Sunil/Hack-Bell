export { SecureUploader } from './components/SecureUploader';
export type {
    SecureUploaderProps,
    PIIType,
    DetectedEntity,
    BoundingBox,
    EvidenceLog,
    ProcessingStage,
    ProcessingState,
} from './types';
